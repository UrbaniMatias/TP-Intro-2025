const conn = require("./db_connection");
const Pagina = require("../models/pagina");

// devuelve todas las paginas
// (la verdad no se para que nos vaya a servir)
async function getPaginas() {
    
}

async function getPaginasByIdAventura(id_aventura) {
    
}

async function getPaginaById(id) {
    
}
